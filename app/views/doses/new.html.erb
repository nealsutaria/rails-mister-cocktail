<%= form_for [@cocktail, @dose] do |f| %>
  <div>
  <%= f.label :description %>
  <%= f.text_field :description %>
</div>

  <%= f.submit %>
<% end %>

<%= link_to "Back", cocktail_path(@cocktail) %>
